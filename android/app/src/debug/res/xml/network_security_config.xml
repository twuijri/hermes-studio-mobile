<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: lets the app talk to a Studio running on the development
     machine over plain HTTP (10.0.2.2 is the host as seen from an emulator).
     Release builds keep Android's default, which refuses cleartext outright. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
